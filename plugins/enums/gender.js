export const gender = {
  MALE: 0,
  FEMALE: 1,
  OTHER: 2,

  getGender(gender) {
    if (gender == this.MALE) return this.MALE
    else if (gender == this.FEMALE) return this.FEMALE
    else if (gender == this.FEMALE) return this.FEMALE
  },
}
